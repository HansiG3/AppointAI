Now create the complete technical design document for my AppointAI project.

Important: This is a **MERN stack project**. Keep the design practical for an MVP/student project. Do not use Python, FastAPI, PostgreSQL, Redis, Next.js, microservices, or other unnecessary technologies.

Use:

* Frontend: React + Vite + JavaScript
* Backend: Node.js + Express.js
* Database: MongoDB + Mongoose
* Authentication: JWT
* AI: LLM API with structured output/function calling
* Styling: CSS / responsive CSS

Do NOT implement the application yet. I only want the system design and planning document.

The document must contain exactly these five major sections:

# 1. System Design (High-Level)

Explain the complete architecture of AppointAI.

Show how these components interact:

User
↓
React Frontend
↓
Node.js + Express Backend
↓
AI Service
↓
Appointment/Doctor/Availability Logic
↓
MongoDB

Also explain the Admin flow:

Admin
↓
React Admin Dashboard
↓
Express Backend
↓
MongoDB

Explain the complete appointment-booking flow:

User enters a natural-language request such as:

"Hey, I want to book a dermatology appointment on 15 August 2026 at 5 PM."

The system should:

1. Receive the user's message.
2. AI understands the request.
3. Extract specialization, date, time, and other relevant information.
4. Check whether required information is missing.
5. Ask the user for missing information.
6. Check doctor and slot availability.
7. If the requested slot is unavailable, find and suggest alternative slots.
8. User selects an available slot.
9. Show the doctor and slot.
10. Ask the user for confirmation.
11. Recheck availability.
12. Create the appointment.
13. Generate a unique Booking ID.
14. Show/send booking confirmation.

Clearly explain this principle:

**AI understands the user's request, but the backend is responsible for validation, availability, and booking.**

The AI must never directly access MongoDB or invent availability.

Include a simple Mermaid high-level architecture diagram and a Mermaid sequence diagram for the booking flow.

Also explain the Admin flow:

Admin → Login → View Appointments → Manage Appointments → Update/Cancel Booking.

---

# 2. Feature Breakdown

Break AppointAI into three categories:

## User Features

Include:

* User registration/login
* AI conversational chat
* Natural-language appointment requests
* Automatic specialization detection
* Date/time extraction
* Missing-information handling
* Doctor search
* Slot availability checking
* Alternative-slot suggestions
* Doctor and slot selection
* Appointment confirmation
* Unique Booking ID
* View appointments
* Modify/reschedule appointment
* Cancel appointment

## AI Features

Include:

* Intent detection
* Entity extraction
* Conversation context
* Missing-information detection
* Clarification questions
* Appointment preference modification
* Backend function/API calling
* Natural-language responses

## Admin Features

Include:

* Admin login
* View appointments
* Search/filter appointments
* View appointment details
* Update/reschedule booking
* Cancel booking
* Manage doctors
* Manage specializations
* Manage doctor availability/slots

Then divide the features into:

### MVP

Features necessary for the first working version.

### Phase 2

Useful features that can be added later.

### Future Enhancements

Advanced features that are not required for the MVP.

Keep this realistic and avoid unnecessary features.

---

# 3. Prompt Design

Design the AI prompting system for AppointAI.

Create a proper **System Prompt** for the AI assistant.

The AI should behave as a healthcare appointment booking assistant.

The prompt should define:

* AI role
* AI responsibilities
* What information the AI should extract
* Required information
* Optional information
* How to handle missing information
* How to handle ambiguous requests
* How to check availability through backend APIs/functions
* How to suggest alternative slots
* How to ask for confirmation
* How to handle modification
* How to handle cancellation

For example:

User:

"I want a skin doctor tomorrow evening."

The AI should understand:

* Skin doctor → Dermatology
* Tomorrow → actual date
* Evening → time range

If an exact time is required, the AI should ask an appropriate clarification question.

Also define the important intents, such as:

* BOOK_APPOINTMENT
* CHECK_AVAILABILITY
* MODIFY_APPOINTMENT
* CANCEL_APPOINTMENT
* VIEW_APPOINTMENT
* GENERAL_QUERY

Design a structured JSON format for the AI's extracted information.

For example:

{
"intent": "BOOK_APPOINTMENT",
"specialization": "Dermatology",
"doctor": null,
"date": "2026-08-15",
"time": "17:00",
"missingInformation": [],
"requiresConfirmation": true
}

Also explain how the Node.js backend should validate this information before performing any operation.

Define the backend functions/APIs the AI can use, for example:

* searchDoctors()
* checkAvailability()
* findAlternativeSlots()
* createAppointment()
* getAppointment()
* modifyAppointment()
* cancelAppointment()

The AI should never directly modify MongoDB.

Include 4–5 realistic example conversations showing:

1. Complete request
2. Missing information
3. Requested slot unavailable
4. Alternative slot selection
5. User modifying their preference

---

# 4. Data Model

Design the MongoDB/Mongoose data model.

Use these main collections:

### User

Include appropriate fields such as:

* _id
* name
* email
* phone
* password
* role
* createdAt

### Admin

If appropriate, either use a separate Admin collection or explain why admin users can be represented using the User collection with roles.

### Doctor

Include:

* name
* specialization
* experience
* qualification
* location
* availability
* status

### Specialization

Examples:

* Dermatology
* Cardiology
* Neurology
* Orthopedics
* Pediatrics

### Slot / Availability

Include:

* doctor
* date
* startTime
* endTime
* status

### Appointment

Include:

* bookingId
* user
* doctor
* specialization
* slot
* date
* time
* status
* createdAt
* updatedAt

Possible statuses:

* PENDING
* CONFIRMED
* CANCELLED
* COMPLETED

### Conversation

Include:

* user
* messages
* role
* message
* timestamp

Explain the relationships between the collections.

Explain how MongoDB should prevent two users from booking the same doctor/slot.

Provide simple Mongoose schema examples or schema structures where useful.

Also provide a Mermaid ER-style relationship diagram if appropriate.

---

# 5. Implementation Plan

Create a step-by-step implementation roadmap for the MERN project.

Break it into phases:

## Phase 1 — Project Setup

* Create React/Vite frontend
* Create Node/Express backend
* Connect MongoDB
* Configure environment variables
* Setup project structure

## Phase 2 — Authentication

* User registration
* User login
* JWT authentication
* Admin authentication
* Role-based authorization

## Phase 3 — Database

Implement:

* User
* Doctor
* Specialization
* Slot
* Appointment
* Conversation

## Phase 4 — Backend APIs

Design the required Express APIs.

Include endpoints such as:

POST /api/auth/register
POST /api/auth/login

POST /api/chat

GET /api/doctors
GET /api/doctors/:id
GET /api/availability

POST /api/appointments
GET /api/appointments
GET /api/appointments/:id
PUT /api/appointments/:id
DELETE /api/appointments/:id

Also include appropriate admin endpoints.

For important APIs explain:

* HTTP method
* Endpoint
* Purpose
* Request body
* Response

## Phase 5 — AI Integration

Explain how the LLM integrates with the Node.js backend.

Use this flow:

User Message
↓
React Chat Interface
↓
POST /api/chat
↓
Node/Express
↓
LLM
↓
Structured Intent + Entities
↓
Backend Validation
↓
Availability/Appointment API
↓
MongoDB
↓
Result
↓
LLM
↓
User

## Phase 6 — Conversational Booking

Implement the exact flow from my original diagram:

Enter request
↓
AI understands request
↓
Required information available?
↓
If No → Ask for missing information
↓
If Yes → Check doctor/slot availability
↓
Requested slot available?
↓
If No → Suggest alternatives
↓
User selects alternative
↓
Show doctor and slot
↓
Ask for confirmation
↓
User confirms?
↓
If No → Modify preferences
↓
If Yes → Create appointment
↓
Generate Booking ID
↓
Send confirmation
↓
Appointment Confirmed

## Phase 7 — Admin Dashboard

Implement:

* Admin login
* Appointment dashboard
* Search/filter
* View appointment
* Update appointment
* Cancel appointment
* Doctor management
* Slot management

## Phase 8 — Testing

Include:

* Backend unit tests
* API testing
* AI extraction testing
* Appointment booking testing
* Availability testing
* Double-booking testing
* Authentication testing
* End-to-end conversational testing

Include test cases for:

* Complete appointment request
* Missing date
* Missing time
* Missing specialization
* Available slot
* Unavailable slot
* Alternative slot
* User changes preference
* User confirms
* User cancels

## Phase 9 — Deployment

Briefly explain how the MERN application can be deployed.

Keep deployment simple and suitable for an MVP.

---

# Final Requirements

The final document should be clear enough that I can use it as the **blueprint for actually building AppointAI**.

Do not over-engineer it.

Do not introduce technologies outside the MERN stack unless absolutely necessary.

Focus on the core idea:

**"Book a healthcare appointment through a conversation instead of navigating through a tedious appointment form."**

Do not create or modify application code yet.

Only generate the five-section technical design document.
